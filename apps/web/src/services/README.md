# Services Exercise Placeholder

This folder is intentionally empty of runtime code.

For the classroom exercise, students can create files here such as:

- `api.ts`
- `events.ts`
- `plans.ts`

Use the Vite proxy with relative URLs like `/api/events`, not hardcoded
`http://localhost:3001` URLs in the frontend.
